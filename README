Still in development ...

This is a Rail 3 application, which shows new messages instantly.

Messages are saved into a Redis database.

A NodeJS server pulls messages from Redis and pushs new messages back to the client.